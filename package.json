{
  "name": "nftfi-marketing-site-singlescroll",
  "version": "1.0.0",
  "description": "A single page scroller using Three.js elements and scroll capture libraries, managed with Taskmaster.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:cf": "vite build && npx wrangler pages deploy dist",
    "deploy:cf:prod": "vite build && npx wrangler pages deploy dist --branch master"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@floating-ui/dom": "^1.7.3",
    "gsap": "^3.13.0",
    "task-master-ai": "^0.22.0",
    "three": "^0.178.0"
  },
  "devDependencies": {
    "vite": "^5.4.20"
  }
}
